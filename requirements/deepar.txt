# DeepAR Model Requirements - PyTorch Backend with GPU Support

# GluonTS with PyTorch backend (for RTX 4050 GPU)
gluonts[torch]==0.14.3

# PyTorch with CUDA support (for GPU acceleration)
torch>=2.0.0
pytorch-lightning>=2.0.0

# Additional dependencies for GluonTS
toolz==0.12.0
pydantic==1.10.13

# For external features preprocessing
scikit-learn>=1.3.0  # For normalization/scaling

# Note: Install PyTorch with CUDA support using:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
